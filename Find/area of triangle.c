#include<stdio.h>

main()
{
 	double A,h,b;
	 float a=0.5;

 	printf("enter the height of triangle:");
 	scanf("%lf",& h);
 	printf("enter the height of base:");
 	scanf("%lf",& b);
 
 	A=a*h*b;
 	printf("%lf",A);
 	
}
